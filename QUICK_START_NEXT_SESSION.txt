================================================================================
GUIDE DE DÉMARRAGE RAPIDE - SESSION SUIVANTE
================================================================================
Projet: ClubManager Platform-API (Migration Prisma/TypeScript)
Date: 20 Janvier 2025
Statut: 44 erreurs TypeScript restantes (réduction de 115 → 44)
================================================================================

┌─────────────────────────────────────────────────────────────────────────┐
│ RÉSUMÉ ULTRA-RAPIDE                                                      │
└─────────────────────────────────────────────────────────────────────────┘

✅ CE QUI FONCTIONNE:
- Routes: /api/products, /api/orders, /api/inventory, /api/messaging, /api/auth
- Prisma Client généré et synchronisé
- Middlewares opérationnels (auth, tenant, validation, errors, audit, etc.)
- Services principaux: product, order, inventory, message

❌ CE QUI RESTE À FAIRE:
- 12 erreurs dans services actifs (order, message, payment, tenant, user)
- 17 routes legacy désactivées (dépendent de mysqlconnector)
- 0% de tests
- Rate limiter en mémoire (à migrer Redis)

┌─────────────────────────────────────────────────────────────────────────┐
│ DÉMARRAGE IMMÉDIAT                                                       │
└─────────────────────────────────────────────────────────────────────────┘

1. VÉRIFIER L'ÉTAT ACTUEL
   cd E:\Developpement\ClubManager\platform-api
   npx tsc --noEmit 2>&1 | grep "error TS" | wc -l
   → Devrait afficher: 44

2. LIRE LE RAPPORT COMPLET
   cat ../TYPESCRIPT_MIGRATION_STATUS.txt
   → Contient l'historique complet et toutes les décisions

3. LANCER LE SERVEUR (si Prisma configuré)
   npm run dev
   → Port 3000 par défaut

┌─────────────────────────────────────────────────────────────────────────┐
│ TÂCHES PRIORITAIRES (PAR ORDRE)                                         │
└─────────────────────────────────────────────────────────────────────────┘

PRIORITÉ 1 - CORRIGER LES 12 ERREURS SERVICES (2-3h)
══════════════════════════════════════════════════════

A) message.service.ts (1 erreur)
   Fichier: src/services/message.service.ts:346
   Erreur: Cannot find name 'updated'
   Fix: Dans markAllAsRead(), utiliser 'result' au lieu de 'updated'

   // Ligne 346 - AVANT:
   count: updated.count,

   // APRÈS:
   count: result.count,

B) order.service.ts (5 erreurs)
   Fichier: src/services/order.service.ts

   Erreur 1 (ligne 245): 'string' not assignable to 'OrderStatus'
   Fix: Typer correctement le status

   Erreur 2 (ligne 260): 'reason' not in scope
   Fix: Ajouter paramètre reason à updateStatus()
   // SIGNATURE:
   async updateStatus(
     id: number,
     status: OrderStatus,
     tenantId: string,
     userId?: number,
     reason?: string  // ← AJOUTER
   )

   Erreur 3 (ligne 262): Cannot find 'newStatus'
   Fix: Utiliser 'status' au lieu de 'newStatus' dans details

   Erreur 4-5 (lignes 422, 447): Too many arguments
   Fix: updateStatus() n'accepte que 4-5 args, supprimer message en trop
   // AVANT:
   await this.updateStatus(id, "confirmée", tenantId, userId, "Order confirmed")
   // APRÈS:
   await this.updateStatus(id, "confirmée" as OrderStatus, tenantId, userId)

   Erreur 6 (ligne 490): countByStatus expects 0-1 args
   Fix: Adapter signature repository OU enlever argument status

C) paymentService.ts (1 erreur)
   Fichier: src/services/paymentService.ts:115
   Erreur: 'null' not assignable to 'string | Date'
   Fix: Utiliser undefined ou nouvelle Date()

   // AVANT:
   datePaiement: data.datePaiement || null,

   // APRÈS:
   datePaiement: data.datePaiement || new Date(),

D) tenantService.ts (4 erreurs)
   Fichier: src/services/tenantService.ts (lignes 67, 89, 257, 278)
   Erreur: Decimal incompatible avec number
   Fix: Mapper Decimal en number lors du return

   // APRÈS chaque tenant query, AVANT le return:
   return {
     ...tenant,
     subscriptions: tenant.subscriptions.map(sub => ({
       ...sub,
       price: Number(sub.price),  // Decimal → number
       plan: {
         ...sub.plan,
         prix: Number(sub.plan.prix)
       }
     }))
   } as TenantWithSubscription;

E) userService.ts (2 erreurs)
   Fichier: src/services/userService.ts

   Erreur 1 (ligne 98): JWT expiresIn type
   Fix: Cast options comme fait dans authTenant.ts
   // AVANT:
   return jwt.sign(payload, secret, { expiresIn: "7d" });
   // APRÈS:
   const expiresIn = "7d";
   return jwt.sign(payload, secret, { expiresIn } as jwt.SignOptions);

   Erreur 2 (ligne 512): OR filter incompatible
   Fix: Vérifier structure Prisma where clause
   // Structure correcte:
   where: {
     tenantId,
     OR: [
       { firstName: { contains: search, mode: 'insensitive' } },
       { lastName: { contains: search, mode: 'insensitive' } },
       { email: { contains: search, mode: 'insensitive' } }
     ]
   }

┌─────────────────────────────────────────────────────────────────────────┐
│ COMMANDES DE VÉRIFICATION                                               │
└─────────────────────────────────────────────────────────────────────────┘

# Vérifier erreurs TypeScript
npx tsc --noEmit

# Compter erreurs
npx tsc --noEmit 2>&1 | grep "error TS" | wc -l

# Voir détails erreurs services actifs
npx tsc --noEmit 2>&1 | grep -E "(message|order|payment|tenant|user)Service"

# Générer Prisma Client (si schema modifié)
npx prisma generate

# Synchroniser DB (développement)
npx prisma db push

┌─────────────────────────────────────────────────────────────────────────┐
│ APRÈS CORRECTION DES 12 ERREURS                                         │
└─────────────────────────────────────────────────────────────────────────┘

PRIORITÉ 2 - DÉCIDER DU SORT DES ROUTES LEGACY (32 erreurs legacy)
═══════════════════════════════════════════════════════════════════

17 routes désactivées dans tsconfig.json:
- utilisateurs, commandes, compte, cours, magasin, professeurs
- messages (legacy), statistiques, informations, alertes
- stocks, echeances, inscription, verification, stripe
- authTenant, paiements, webhooks
- auth/password-reset, server.ts

OPTIONS:
A) RÉÉCRIRE avec Prisma (recommandé pour critiques)
   - utilisateurs → créer user.routes.ts (Prisma)
   - Temps estimé: 2-3h par route

B) SUPPRIMER si non utilisées
   - Vérifier logs production d'abord
   - rm src/routes/[fichier].ts

C) CRÉER ADAPTERS temporaires (déconseillé)
   - Wrapper Prisma pour interface legacy
   - Solution temporaire only

PRIORITÉ 3 - TESTS
══════════════════

Actuellement: 0% couverture

1. Setup framework
   npm install --save-dev vitest @vitest/ui supertest

2. Créer tests unitaires validators
   - src/__tests__/validators/shop.validator.test.ts

3. Tests intégration repositories
   - Utiliser DB test ou SQLite in-memory

4. Tests E2E routes principales
   - /api/products CRUD
   - /api/orders flow complet
   - /api/auth login/register

┌─────────────────────────────────────────────────────────────────────────┐
│ FICHIERS IMPORTANTS À CONNAÎTRE                                         │
└─────────────────────────────────────────────────────────────────────────┘

ARCHITECTURE:
src/
├── app.ts                    ← Point d'entrée principal
├── routes/
│   ├── api.ts               ← Montage routes (legacy commentées)
│   ├── products.ts          ✅ Route moderne
│   ├── orders.ts            ✅ Route moderne
│   ├── inventory.ts         ✅ Route moderne
│   └── messaging.ts         ✅ Route moderne
├── services/
│   ├── product.service.ts   ✅ Migré
│   ├── order.service.ts     🔄 5 erreurs à corriger
│   ├── message.service.ts   🔄 1 erreur à corriger
│   ├── paymentService.ts    🔄 1 erreur
│   ├── tenantService.ts     🔄 4 erreurs
│   └── userService.ts       🔄 2 erreurs
├── repositories/            ✅ Tous migrés Prisma
├── middleware/              ✅ Tous opérationnels
└── types/                   ✅ Bien définis

CONFIGURATION:
prisma/schema.prisma         ← Schéma DB (synchronisé)
tsconfig.json                ← Exclusions legacy
package.json                 ← Scripts et dépendances
.env                         ← Variables d'environnement

┌─────────────────────────────────────────────────────────────────────────┐
│ DÉCISIONS ARCHITECTURALES CLÉS                                          │
└─────────────────────────────────────────────────────────────────────────┘

1. tenantId = STRING (cuid/UUID)
   → Partout dans le code, pas de number

2. Modèles Prisma en français (Article, Commande)
   → Types TypeScript en anglais (Product, Order)
   → Mapping dans repositories

3. AuditAction = ENUM étendu
   → 45 actions définies (PRODUCT_CREATE, ORDER_UPDATE, etc.)
   → Toujours fournir: action, resource, resourceType, tenantId

4. Rate limiter = In-memory (DEV ONLY)
   → TODO: Migrer Redis pour production

5. Error handling = Global + custom classes
   → NotFoundError, ShopValidationError
   → Format: {success, data/error, timestamp}

┌─────────────────────────────────────────────────────────────────────────┐
│ VARIABLES D'ENVIRONNEMENT REQUISES                                      │
└─────────────────────────────────────────────────────────────────────────┘

Fichier: .env

DATABASE_URL="mysql://user:password@localhost:3306/clubmanager"
JWT_SECRET="your-super-secret-key-change-in-production"
JWT_EXPIRES_IN="7d"
NODE_ENV="development"
PORT=3000

OPTIONNEL:
REDIS_URL="redis://localhost:6379"  # Pour rate limiter (future)
SENTRY_DSN=""                       # Pour monitoring (future)

┌─────────────────────────────────────────────────────────────────────────┐
│ PROMPT SUGGÉRÉ POUR PROCHAINE SESSION                                   │
└─────────────────────────────────────────────────────────────────────────┘

"Je continue la migration TypeScript du projet ClubManager.
Il reste 44 erreurs TypeScript dont 12 dans les services actifs.
J'ai lu le rapport TYPESCRIPT_MIGRATION_STATUS.txt.

Priorité: Corriger les 12 erreurs dans les services suivants:
- message.service.ts (1 erreur)
- order.service.ts (5 erreurs)
- paymentService.ts (1 erreur)
- tenantService.ts (4 erreurs)
- userService.ts (2 erreurs)

Peux-tu corriger ces erreurs en suivant les fixes suggérés dans
QUICK_START_NEXT_SESSION.txt ?"

┌─────────────────────────────────────────────────────────────────────────┐
│ MÉTRIQUES ACTUELLES                                                      │
└─────────────────────────────────────────────────────────────────────────┘

Erreurs TypeScript: 44 (réduction de -62% depuis le début)
├─ Services actifs: 12 (27%)
├─ Legacy imports: 28 (64%)
└─ Types: 4 (9%)

Routes migrées: 6/23 (26%)
├─ Modernisées: products, orders, inventory, messaging, auth, health
└─ Legacy désactivées: 17 fichiers

Services migrés: 8/15 (53%)
Middlewares: 11/11 (100%)
Tests: 0/0 (0%)

Estimation temps restant: 3-4 semaines (1 dev full-time)

┌─────────────────────────────────────────────────────────────────────────┐
│ RESSOURCES UTILES                                                        │
└─────────────────────────────────────────────────────────────────────────┘

Documentation complète: TYPESCRIPT_MIGRATION_STATUS.txt (541 lignes)
Thread historique: Disponible dans Zed (toutes les décisions documentées)
Prisma docs: https://www.prisma.io/docs
Express docs: https://expressjs.com
Zod validation: https://zod.dev

================================================================================
BONNE CONTINUATION ! 🚀
================================================================================
Le gros du travail est fait. Les 12 erreurs services sont mineures et rapides
à corriger. Après ça, décider du sort des routes legacy et ajouter les tests.
================================================================================
