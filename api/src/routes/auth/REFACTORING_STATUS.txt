================================================================
REFACTORING auth.resolvers.ts - STATUT
================================================================

✅ DEJA FAIT (dans le fichier actuel):
  - Import des middleware et custom errors (lignes 26-35)
  - Remplacement interface Context par AuthContext (ligne 37)
  - verifyAuth → utilise requireAuth() (ligne 78)
  - verifyResetToken → utilise custom errors (lignes 97-153)

🔄 EN COURS / A FAIRE:

MUTATIONS:
  [ ] login (ligne 254)
      Remplacer: GraphQLError → ValidationError + InvalidCredentialsError
      Remplacer: catch block → toAuthError(error)
  
  [ ] forgotPassword (ligne 458)
      Remplacer: GraphQLError → ValidationError + UserNotFoundError
      Remplacer: catch block → toAuthError(error)
  
  [ ] resetPassword (ligne 516)
      Remplacer: GraphQLError → ValidationError + TokenExpiredError
      Remplacer: catch block → toAuthError(error)
  
  [ ] refreshToken (ligne 563)
      Ajouter: requireAuth() wrapper
      Remplacer: catch block → toAuthError(error)

QUERIES:
  [✅] verifyAuth → FAIT avec requireAuth
  [✅] verifyResetToken → FAIT avec custom errors
  [ ] confirmEmail → Peut utiliser ValidationError
  [✅] checkAuthStatus → OK tel quel (pas besoin auth)
  [✅] testAuth → OK tel quel (endpoint public)

================================================================
PROCHAINE ETAPE RECOMMANDEE:
================================================================

Plutot que de tout refactorer maintenant, je suggere:

1. TESTER ce qui est deja fait:
   - verifyAuth avec requireAuth
   - verifyResetToken avec custom errors
   
2. UTILISER les nouveaux outils dans NOUVEAUX resolvers

3. Refactorer le reste PROGRESSIVEMENT au besoin

Les outils sont prets et fonctionnels. Le fichier example
(auth.resolvers.refactored.example.ts) montre comment faire.

================================================================
