Parfait ! Voici le **RAPPORT COMPLET DES TODOs** avec plan d'action :

---

## 📋 **RAPPORT D'AUDIT DES TODOs**

J'ai trouvé **~50 TODOs** répartis en 12 catégories. Voici la priorisation :

---

### 🔴 **PRIORITÉ HAUTE** (Critique - à faire maintenant)

#### **1. Nettoyer les TODOs Redux obsolètes** ⚠️
**Problème:** ~15 fichiers ont des commentaires `// TODO: Setup Redux` alors que vous utilisez **Zustand** !

Fichiers concernés:
- `LoginPage.tsx`, `RegisterPage.tsx`, `InscriptionPage.tsx`
- `ParticipantsPage.tsx`, `magasin.tsx`, `checkout.tsx`
- `ReduxDebugger.tsx`, `CheckoutForm.tsx`

**Action:** Supprimer tous ces commentaires obsolètes qui créent de la confusion.

---

#### **2. Créer `authService` & `userService`** 🔧
**Fichier:** `ClubManager/front-end/src/core/index.ts`

```typescript
// TODO: Export services when implemented
// export { authService } from "./services/auth";
// export { userService } from "./services/user";
```

**Action:** Créer ces services manquants (ils sont référencés mais n'existent pas).

---

#### **3. Backend: Implémenter `getAlerts` monitoring** 🚨
**Fichier:** `ClubManager/api/src/routes/settings/core/resolvers/monitoring.resolvers.ts`

```typescript
// TODO: Implement get alerts from database
// const alerts = await monitoringService.getAlerts(status);
```

**Action:** Connecter le resolver au service d'alertes.

---

#### **4. Backend: Finaliser notifications email/admin** 📧
**Fichiers:**
- `circuit-breaker.ts`: TODO notification (Slack/Discord)
- `email-queue-worker.ts`: TODO alerter admin
- `sendgrid-sender.ts`: TODO décommenter sauvegarde DB Prisma

**Action:** Implémenter les alertes admin pour les échecs critiques.

---

### 🟡 **PRIORITÉ MOYENNE** (Important)

#### **5. Créer barrel exports manquants** 📦
**Fichier:** `shared/components/index.ts`

TODOs pour créer `index.ts` dans:
- `/forms` folder
- `/modals` folder
- `/ui` folder
- `/debug` folder
- Footer component

**Action:** Créer les barrel exports pour améliorer l'expérience dev.

---

#### **6. Intégrer error tracking (Sentry)** 📊
**Fichiers:**
- `ErrorBoundary.tsx`: TODO Send to Sentry
- `logger.ts`: TODO Integrate with Sentry

**Action:** Configurer Sentry pour la production.

---

#### **7. Backend: Rate limiting service** 🛡️
**Fichier:** `api/src/shared/services/index.ts`

```typescript
// export * from "./rate-limit.service.js"; // TODO: Create rate-limit.service.ts
```

**Action:** Créer le service de rate limiting.

---

#### **8. i18n setup** 🌍
**Fichier:** `store/uiStore.ts`

```typescript
// TODO: Update i18n instance if using i18next
```

**Action:** Connecter i18next au store UI.

---

### 🟢 **PRIORITÉ BASSE** (Backlog)

#### **9. Fix CSS paths** 🎨
4 occurrences de `// import '../styles/connexion.css'; // TODO: Fix CSS path`

#### **10. Vérifier stub hooks** ✅
`useInscriptions.ts` & `usePaiements.ts` - Vous avez dit qu'ils sont implémentés.

#### **11. Add missing schemas** 📝
`datareservationSchema` manquant dans `@clubmanager/types`.

---

## 🎯 **PLAN D'ACTION RECOMMANDÉ**

Voici ce que je vous propose de faire **maintenant** :

### **Option A: Quick Wins (1-2h)** ⚡
1. ✅ Supprimer tous les TODOs Redux obsolètes (nettoyage)
2. ✅ Créer barrel exports manquants (DX)
3. ✅ Fix CSS paths ou supprimer imports

### **Option B: Services critiques (2-4h)** 🔧
1. ✅ Créer `authService` & `userService`
2. ✅ Implémenter getAlerts monitoring (backend)
3. ✅ Finaliser email alerting (backend)

### **Option C: Production-ready (4-8h)** 🚀
1. ✅ Intégrer Sentry (ErrorBoundary + logger)
2. ✅ Rate limiting service (backend)
3. ✅ i18n setup complet

---

## ❓ **QUELLE OPTION CHOISISSEZ-VOUS ?**

**A.** Quick Wins (nettoyage + barrel exports) — **Recommandé pour commencer**
**B.** Services critiques (authService + monitoring)
**C.** Production-ready (Sentry + rate limiting + i18n)
**D.** Custom (vous me dites quels TODOs résoudre)

Je recommande **Option A** pour démarrer, car elle élimine ~20 TODOs rapidement et améliore la lisibilité du code ! 🎯

Quelle option voulez-vous ?
