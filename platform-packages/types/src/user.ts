export interface User {
  id: number;
  first_name: string;
  last_name: string;
  nom_utilisateur: string;
  email: string;
  genres: string;
  status: string;
  grades: string;
  abonnement: string;
  date_of_birth: string; // Format ISO (YYYY-MM-DD)
}
