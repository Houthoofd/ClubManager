import { describe, it, expect } from 'vitest';
import { LastProfessorWarningModal } from './LastProfessorWarningModal';

describe('LastProfessorWarningModal', () => {
  it('should be defined', () => {
    expect(LastProfessorWarningModal).toBeDefined();
  });

  it('should have expected structure', () => {
    expect(LastProfessorWarningModal).toBeTruthy();
  });
});
